[package]
name = "rustc_wrapper"
version = "0.1.0"
authors = ["David Semakula <hello@davidsemakula.com>", "Use Ink <ink@use.ink>"]
edition = "2021"
publish = false

[[bin]]
name = "rustc_wrapper"
path = "main.rs"

[dependencies]

# todo Remove once the `polkadot-sdk` compilation error
# for `tokio` is fixed (https://github.com/use-ink/ink/pull/2557).
[build-dependencies]
tokio = { version = "1", features = ["net"] }

[workspace]
# Tells cargo that this crate is not part of the workspace.
